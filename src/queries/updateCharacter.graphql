mutation UpdateCharacter($id: ID!, $code: String!) {
  updateCharacter(id: $id, code: $code) {
    id
    birthday
    corporation {
      id
      name
      ticker
      alliance {
        id
        name
        ticker
      }
    }
    name
    scopes
    securityStatus
    totalSp
  }
}
