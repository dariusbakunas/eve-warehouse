query GetSkillGroupSkills($characterId: ID!, $skillGroupId: ID!) {
  character(id: $characterId) {
    id
    skillGroup(id: $skillGroupId) {
      id
      skills {
        id
        name
        multiplier
        activeSkillLevel
        trainedSkillLevel
        skillPointsInSkill
      }
    }
  }
}
