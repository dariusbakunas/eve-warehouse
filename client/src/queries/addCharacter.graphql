mutation AddCharacter($code: String!) {
  addCharacter(code: $code) {
    id
    birthday
    corporation {
      id
      name
      ticker
      alliance {
        id
        name
        ticker
      }
    }
    name
    scopes
    securityStatus
    totalSp
  }
}
